server:
  port: 9090

spring:
  application:
    name: api-gateway
  
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true # If using Eureka in future

# Logging configuration for our filter
logging:
  level:
    root: INFO
    com.loganalyzer.gateway: DEBUG
    org.springframework.cloud.gateway: INFO