server:
  port: 8089

spring:
  application:
    name: log-ingestion-service
  
  data:
    mongodb:
      uri: mongodb+srv://12345678:3plqvKOO50bXsbTo@pop.114ym.mongodb.net/log_analyzer?retryWrites=true&w=majority
      database: log_analyzer
    
    elasticsearch:
      uris: http://localhost:9200

  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        # FIXED: Type headers ko true rakhein taaki Parsing service object identify kar sake
        spring.json.add.type.headers: true 
        # Optional: Agar headers ke bina bhi map karna ho
        spring.json.type.mapping: "logEvent:com.loganalyzer.common.dto.LogEvent"

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

logging:
  level:
    com.loganalyzer: DEBUG
    org.springframework.kafka: INFO