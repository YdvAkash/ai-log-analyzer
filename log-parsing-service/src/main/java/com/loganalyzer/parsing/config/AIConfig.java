package com.loganalyzer.parsing.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AIConfig {

    // Spring AI starter automatically Gemini ChatClient create kar deta hai
    // Hum yahan custom settings (temperature, etc.) add kar sakte hain.
}